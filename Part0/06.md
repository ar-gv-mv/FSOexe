sequenceDiagram
    participant browser
    participant server

    Note right of browser: user enters text into the form field and clicks the Save button
    Note right of browser: browser sends the new note to the server in JSON

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of server: the server saves the new note
    server-->>browser: 201 Created
    deactivate server

    Note right of browser: There is no redirect or page reload. The browser stays on the same page and updates the notes list
